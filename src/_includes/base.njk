<!doctype html>
<html lang="en">
  <head>
    {% include "partials/head-content.njk" %}
  </head>
  <body class="h-entry">
    <main class="main grid prose {% if wideLayout %}prose--wide{% endif%}">
      {% if title %}
        <header class="prose__header">
          <h1 class="p-name fancy-headline">{{ title|safe }}</h1>
        </header>
        <small>
          <time class="dt-published" datetime="{{ page.date }}">
          {{ page.date|formatDate }}
          </time>
        </small>
      {% endif %}
      {{ content|safe }}
      {% if not noBlurb %}
        <aside class="gc-aside gr-blurb">
          <div class="prose">
            <p>
              <a href="/">
                <span class="avatar circle">
                  <img alt="Frederic Marx" class="u-photo" src="/frederic-marx.jpg" height="60" width="60">
                </span>
              </a>
            </p>
            <p>My name is
            {% if gay.environment == 'gay' %}
              <a href="https://software.gay" rel="me">
            {% else %}
              <a class="p-author h-card" href="https://fmarx.com" rel="author">
            {% endif %}
              Frederic Marx</a>. I&nbsp;am a front-end developer building websites that support and inspire music makers at <a href="https://www.ableton.com/">Ableton</a> in&nbsp;Berlin.</p>
          </div>
        </aside>
      {% endif %}
    </main>
    <div class="grid">
      <div class="gc-main">
        {% include "partials/footer.njk" %}
      </div>
    </div>
  </body>
</html>
